{
  "name": "student-registration-system",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "api": "php -S 127.0.0.1:8000 -t api api/index.php",
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  }
}
